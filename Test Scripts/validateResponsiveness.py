# Resize the browser window to different sizes (small, medium, large)
driver.set_window_size(480, 800)  # Small size
# Add assertions or checks here based on the expected behavior

driver.set_window_size(1024, 768)  # Medium size
# Add assertions or checks here based on the expected behavior

driver.set_window_size(1440, 900)  # Large size
# Add assertions or checks here based on the expected behavior
